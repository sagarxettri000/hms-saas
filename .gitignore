# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
.next/
out/
coverage/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local
.env.production
!.env.example
# Vercel CLI pull artifacts (contain OIDC/session tokens)
.env.vercel

# Runtime / storage (only the top-level storage dir, not modules/storage)
/storage/
backups/
logs/
*.log
npm-debug.log*

# Ad-hoc command/output artifacts
build.txt
build_output.txt
.freebuff/
test_output.txt

# Prisma (migrations are version-controlled and MUST be committed)

# Supabase local link cache (metadata only, regenerate via `supabase link`)
supabase/.temp/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

.vercel
